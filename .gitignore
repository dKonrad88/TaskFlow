# Backups locais do index.html (não versionar — são cópias de segurança da máquina)
backups/

# Estado local do Claude Code (memória/sessões — específico de cada máquina).
# A memória COMPARTILHADA entre PC e Mac é o CLAUDE.md na raiz, esse sim versionado.
.claude/

# Cópia local de recuperação (o app canônico é o index.html)
taskflow.html

# Sistema operacional
.DS_Store
Thumbs.db
desktop.ini
